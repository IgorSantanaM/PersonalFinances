import React, {useState} from 'react';

export default function Category(){
    const[accountType, setAccountType] = useState('Income');
    const options  = ['Income', 'Expense', 'Transfer', 'Loan payment', 'Loan armotization'];

    return(
        <h1>balbla</h1>
    );
}